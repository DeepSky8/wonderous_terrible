import React from "react";
import { Outlet } from "react-router-dom";

const ExploreSpace = () => {

    return (
        <div>
            <Outlet />
        </div>
    )
}

export default ExploreSpace